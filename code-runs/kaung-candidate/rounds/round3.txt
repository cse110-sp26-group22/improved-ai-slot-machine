The reels are STILL blank. The translateY animation approach is not working. Please completely rewrite the reel system using a simpler method.

Instead of translateY on a giant strip of cloned symbols, use this approach:
- Each reel div contains exactly 3 symbol divs (top, middle, bottom)
- On load, populate each reel with 3 random symbols from the reel config
- When spinning: use a setInterval to rapidly cycle random symbols in each reel for 1-2 seconds (like a slot machine flickering), then stop each reel one at a time with a slight delay between them, landing on the final result
- The middle row is the payline

This is much simpler and more reliable than trying to calculate pixel positions on a scrolling strip. Please rewrite script.js with this approach and update the CSS accordingly. The reels container height and symbol sizing need to match - each symbol should be about 80px tall and the container should show exactly 3 rows.

Do NOT keep the old translateY system. Replace it entirely.